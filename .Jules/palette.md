## 2024-05-22 - Dynamic Tooltips for Toggles
**Learning:** Toggle buttons (like Favorites) require dynamic tooltips that reflect the current state (e.g., 'Add to Favorites' vs 'Remove from Favorites') rather than a static label.
**Action:** When adding toggle buttons, ensure the tooltip changes based on the state to provide accurate context for screen readers.
