## 2024-05-22 - [Custom Back Button Accessibility]
**Learning:** Custom navigation headers often miss standard accessibility features like tooltips and semantic labels, making them invisible or confusing to screen reader users.
**Action:** Always verify that `IconButton`s in custom headers have a `tooltip` or `semanticsLabel` set, especially for navigation actions.
